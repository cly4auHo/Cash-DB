CREATE DATABASE itea; \u0438\u043c\u044f \u0431\u0430\u0437\u044b

CREATE TABLE users (id INT, name VARCHAR(30), age INT, address VARCHAR(3)); òàáëèöà

DESCRIBE users; ïîêàçàòü òàáëèöó

INSERT INTO users (name, age, address) VALUES ("Alex",13,"Kiev") äîáàâèòü ýëåìåíò â òàáëèöó

SELECT * FROM users; âûáîðêà (áåç êðèòåðèÿ)

SELECT name, age FROM users WHERE age > 30; âûáîðêà ñ ôèëüòðàöèåé (íå âñå òàáëèöû + ôèëüòðàöèÿ if)

SELECT name, age FROM users WHERE age > 30 AND id = 2; (&&)


